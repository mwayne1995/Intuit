L1 = [1,10,5,63,29,71,10,12,44,29,10,-1]

def sort(L):
    res = []
    count = 0
    while L:
        m = L[0]
        for i in range(0, len(L)):
            if L[i] < m:
                m = L[i]
        res.append(m)
        L.remove(m)
            
    return res
    
sort(L1)
